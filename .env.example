# ZQLZ Build Configuration
# Copy this file to .env and fill in the values as needed

# ============================================================================
# Release Channel Configuration
# ============================================================================
# Controls the build mode, app name, and icon variant
# Options: stable | nightly | dev (default)
#
# stable:  "ZQLZ" - Uses base icon, release build
# nightly: "ZQLZ Nightly" - Uses -nightly icon if available, release build
# dev:     "ZQLZ Dev" - Uses -dev icon if available, debug build
RELEASE_CHANNEL=stable

# ============================================================================
# Version Configuration
# ============================================================================
# Version number for the application bundle
# Format: MAJOR.MINOR.PATCH
ZQLZ_VERSION=0.1.0

# ============================================================================
# macOS Code Signing (Optional)
# ============================================================================
# Required for distribution outside of development machines
# Leave empty to skip signing (for local testing only)

# Base64-encoded .p12 certificate file
# To encode: base64 -i path/to/certificate.p12 | pbcopy
MACOS_CERTIFICATE=

# Password for the .p12 certificate
MACOS_CERTIFICATE_PASSWORD=

# ============================================================================
# Apple Notarization (Optional)
# ============================================================================
# Required for notarization (allows users to bypass Gatekeeper warnings)
# All three values must be provided to enable notarization

# Base64-encoded AuthKey .p8 file from App Store Connect
# To encode: base64 -i path/to/AuthKey_XXXXXXXXXX.p8 | pbcopy
APPLE_NOTARIZATION_KEY=

# Key ID from App Store Connect (e.g., "ABCD123456")
APPLE_NOTARIZATION_KEY_ID=

# Issuer ID from App Store Connect (UUID format)
APPLE_NOTARIZATION_ISSUER_ID=
