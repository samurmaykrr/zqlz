#!/usr/bin/env bash
#
# Release versions are bumped manually via ./script/bump-version and merged
# through pull requests. This hook intentionally does not mutate Cargo.toml.

set -euo pipefail

exit 0
